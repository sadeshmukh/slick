{
  "name": "slick",
  "private": true,
  "scripts": {
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:sh": "git ls-files '*.sh' | xargs shellcheck -S warning",
    "validate": "node scripts/ci/validate.js"
  },
  "devDependencies": {
    "oxfmt": "^0.53.0",
    "oxlint": "^1.0.0"
  }
}
